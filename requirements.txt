# ============================================================
# JobHunter 后端依赖
# 安装: pip install -r requirements.txt
# ============================================================

# ---------- Web 框架 ----------
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# ---------- 配置 & 数据校验 ----------
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1

# ---------- 数据库 ORM ----------
SQLAlchemy==2.0.35
aiomysql==0.2.0          # 异步 MySQL 驱动
PyMySQL==1.1.1           # 同步驱动(脚本/迁移用)

# ---------- Elasticsearch ----------
elasticsearch==8.15.1

# ---------- 知识图谱 Neo4j ----------
# py2neo: Pythonic 客户端(开发期/MVP 用, 上手快)
py2neo==2021.2.4

# ---------- 向量数据库(RAG/语义匹配) ----------
# Windows 安装注意: chroma-hnswlib 需要 C++ 编译, 用预编译包安装:
#   pip install --only-binary :all: chromadb
chromadb

# ---------- 智谱 GLM(岗位推荐: 向量化 + LLM 重排) ----------
# zhipuai: 官方 Python SDK, 封装了鉴权/重试
zhipuai

# ---------- 认证 ----------
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# ---------- HTTP 客户端 ----------
httpx==0.27.2

# ---------- 日志 ----------
loguru==0.7.2

# ---------- Neo4j 官方驱动 ----------
# (队友知识图谱代码按 6.x API 编写, 版本约束统一放这, 勿与他处重复声明)
neo4j>=6.0,<7.0