# 测试账号说明

> 种子账号由 `db/mysql/02_seed.sql` 生成（密码统一 **123456**）。
> 空库部署时经登录页「首次部署？初始化系统」自动创建（含演示数据）；
> 已有环境重跑种子会同步密码（幂等）。

## 账号一览

| 账号 | 手机号 | 角色 | 用途 |
|---|---|---|---|
| 管理员 | `13800000000` | admin | **数据管理页**（一键同步/预览爬虫数据），后台全权限 |
| 求职者A | `13900000001` | user | **演示主力**：自带简历/技能/投递记录（03_mock），可演示推荐、简历管理、求职进度 |
| 新用户 | `13900000002` | user | **空白账号**：演示首次使用流程（注册后上传简历 → 解析 → 推荐） |

- 密码统一：`123456`
- 登录方式：手机号或邮箱均可（邮箱格式 `admin@jobhunter.local` / `user1@` / `user2@`）

## 各账号典型演示路径

### 管理员（13800000000）
```
登录 → 数据管理 → 一键同步所有库(MySQL/ES/向量/图谱)
       → Dashboard 查看统计与新兴技能发现
```

### 求职者A（13900000001）
```
登录 → 我的简历(已有解析好的简历+技能标签)
     → 岗位推荐(有简历数据, 匹配效果直观)
     → 个人中心(收藏/求职进度有 mock 记录)
```

### 新用户（13900000002）
```
登录(空账号) → 上传简历 → LLM 解析(约1分钟) → 技能标签展示
            → 生成推荐 → 体验完整新用户旅程
```

## 注意

- 正式/公开环境部署后**立即修改这三个账号的密码**
- 求职者A 的简历来自 `03_mock_data.sql`（仅首次初始化时导入；一键同步不会重复导入 mock）
