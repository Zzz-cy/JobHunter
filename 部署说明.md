# JobHunter 部署运行指南

> **只需要安装 Docker Desktop**,Python/Node/MySQL/ES/Neo4j 等全部运行在容器内,本机零依赖。

---

## 一、环境要求

- **Docker Desktop**(Windows/Mac)或 Docker + Compose v2(Linux)
- 可用内存建议 **6GB 以上**(MySQL + Elasticsearch + Neo4j 是内存大户;内存紧张可关闭 Neo4j,见"常见问题")
- 端口 80 未被占用

## 二、提交包内容确认

解压后请确认以下文件存在(代码 + 数据 + 配置):

```
JobHunter/
├── .env                        # 环境配置(含演示用大模型 API Key)
├── db/data/                    # 岗位数据(10,000 条真实爬取)
├── docker-compose.yml          # 六容器编排
├── evaluation/real/            # 真实简历评测(含 13 份简历与人工标注答案)
└── ...
```

## 三、一键启动

```bash
# 在解压后的项目根目录执行(首次构建约 5~10 分钟, 取决于网络)
docker compose up -d --build

# 确认 6 个容器都在运行
docker compose ps
```

启动的容器:frontend(前端, Nginx, 80 端口)/ backend(业务后端)/ llm(大模型引擎)/ mysql / elasticsearch(内置 IK 中文分词)/ neo4j(知识图谱)。

## 四、初始化(两步, 约 3~5 分钟)

### 第 1 步:系统初始化

浏览器打开 `http://localhost`,在登录页点击 **"首次部署?初始化系统"**——自动完成建库建表、技能字典种子、演示账号创建(免登录自毁式接口,成功后自动失效)。

### 第 2 步:导入岗位数据(一键同步四库)

用管理员账号登录(手机号 `13800000000`,密码 `123456`),进入 **数据管理** 页,点击 **"一键同步所有库"**——一条链路完成:

```
MySQL 导入 10,000 条岗位 → ES 检索索引重建 → ChromaDB 向量库构建 → Neo4j 知识图谱重建
```

页面实时显示四步进度条,全部变绿即完成。

## 五、功能实现路径

| 功能 | 路径 |
|---|---|
| 职位检索 | 首页/职位列表:关键词搜索 + 多维筛选 |
| 简历 AI 解析 | 登录 `13900000002`(新用户,密码 123456)→ 简历管理 → 上传 PDF |
| 岗位推荐 | 登录 `13900000001`(求职者,自带简历)→ 岗位推荐 → 开始推荐 |
| 知识图谱 | 知识图谱页:切换岗位方向,查看核心技能/相似方向子图,可按关系类型筛选 |
| 数据大盘 | 数据分析页:新兴技能双榜/需求趋势/技能演化/经验×薪资雷达 |
| 新兴技能闭环 | 管理员 → 数据大盘"新兴技能"Tab → 勾选候选词 → 转正所选 → 职位搜索该词验证生效 |
| 新岗位发现 | 新岗位发现页 → 触发"发现新岗位"→ 查看生成的岗位定义四要素 → 管理员可在线修正 |
| AI 求职顾问 | 登录后进入"AI 顾问",对话式差距分析与学习路径规划 |

## 六、演示账号

| 账号(手机号) | 角色 | 用途 |
|---|---|---|
| `13800000000` | 管理员 | 数据管理、新兴技能转正、新岗位发现触发与修正 |
| `13900000001` | 求职者 | 自带解析好的简历/技能/投递记录,直接演示推荐 |
| `13900000002` | 新用户 | 空白账号,演示注册→上传简历→AI 解析→推荐完整流程 |

密码统一 `123456`,手机号或邮箱均可登录(`admin@jobhunter.local` / `user1@jobhunter.local` / `user2@jobhunter.local`)。

## 七、评测材料

核心指标(JD 解析 91.9% / 简历提取 96.0% / 人岗匹配 Precision@5 90.8%)的测试方法与结果见随包提交的《JobHunter测试文档.pdf》;真实简历评测材料(简历原件与人工标注答案)在 `evaluation/real/` 内(含个人隐私)。

## 八、常见问题

**Q: 清空数据重新部署?**
```bash
docker compose down -v      # 删除数据卷
docker compose up -d --build
```
然后重新执行第四节初始化两步。

